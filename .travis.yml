language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-coffee-node' ]; then cd .. && eval "mv $currentfolder generator-coffee-node" && cd generator-coffee-node; fi

